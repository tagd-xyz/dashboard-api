name: direction
in: path
required: false
description: >
  Sort order:
    * asc - Ascending, from A to Z.
    * desc - Descending, from Z to A.
schema:
  type: string
  enum: [asc, desc]
  example: asc
